# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET   /v1/metadata        v1.controllers.MetadataController.getMetadata(ids)
POST  /v1/metadata        v1.controllers.MetadataController.getMultiMetadata